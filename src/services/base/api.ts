export const getUserInfo = async (): Promise<any> => {
  return null;
};

export const getPermission = async (): Promise<any[]> => {
  return [];
};

export const initOneSignal = async (): Promise<any> => {
  return null;
};

export const deleteOneSignal = async (): Promise<any> => {
  return null;
};
